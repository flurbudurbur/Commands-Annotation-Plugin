package test;

import dev.flur.commands.CommandInfo;

/**
 * Test command with only the required name attribute.
 * This tests the default values for all other attributes.
 */
@CommandInfo(name = "minimal")
public class MinimalCommand {
    // Command implementation would go here in a real command
}